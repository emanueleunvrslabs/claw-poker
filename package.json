{
  "name": "claw-poker",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter web build",
    "test": "pnpm --filter server test:run"
  }
}
